$kind: http-request
url: "{{base_fhir}}/Encounter?subject=Patient/{{patient_id}}&_count=50"
method: GET
headers:
  Authorization: Bearer {{token}}
  Accept: application/fhir+json
queryParams:
  subject: Patient/{{patient_id}}
  _count: "50"
order: 2000
